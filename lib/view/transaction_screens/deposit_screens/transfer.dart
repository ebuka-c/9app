import 'package:flutter/material.dart';

class TransferDeposit extends StatelessWidget {
  const TransferDeposit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
