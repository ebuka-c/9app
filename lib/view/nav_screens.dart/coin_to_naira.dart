import 'package:flutter/material.dart';

class CoinToNaira extends StatelessWidget {
  const CoinToNaira({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CoinToNaira'),
      ),
    );
  }
}
